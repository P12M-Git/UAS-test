console.error('Driver_Performance/ is an archived copy, not the deployable application. Set Render Root Directory to the repository root (blank), then npm ci --include=dev && npm run build; npm start.');
process.exit(1);
